{
  "entities": {
    "BrandProfile": {
      "title": "Brand Profile",
      "description": "User's brand identity and niche configuration.",
      "type": "object",
      "properties": {
        "niche": { "type": "string", "description": "The specific market niche of the brand." },
        "tone": { "type": "string", "description": "The voice and tone of social media content." },
        "twitterApiKey": { "type": "string", "description": "Hashed or encrypted Twitter API Key." },
        "linkedinApiKey": { "type": "string", "description": "Hashed or encrypted LinkedIn API Key." },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["niche", "tone"]
    },
    "Post": {
      "title": "Social Media Post",
      "description": "A post generated by AI, either draft, scheduled, or published.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "title": { "type": "string" },
        "content": { "type": "string" },
        "platform": { "type": "string", "enum": ["LinkedIn", "Twitter", "Cross-platform"] },
        "status": { "type": "string", "enum": ["draft", "scheduled", "published"] },
        "scheduledTime": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "content", "platform", "status"]
    }
  },
  "firestore": {
    "/brandProfiles/{userId}": {
      "schema": "BrandProfile",
      "description": "Store individual brand settings for each user."
    },
    "/posts/{postId}": {
      "schema": "Post",
      "description": "Store all social media posts."
    }
  }
}
